{
  "name": "wooffet-labs-site",
  "version": "1.0.0",
  "private": true,
  "description": "Wooffet Labs Site",
  "author": "wooffet",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "npm ci && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "f-login": "firebase login",
    "f-init": "firebase init",
    "f-deploy": "firebase deploy",
    "g-version": "gatsby --version"
  },
  "dependencies": {
    "gatsby": "^3.13.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "firebase-tools": "^9.16.0",
    "gatsby-cli": "^3.13.0"
  }
}